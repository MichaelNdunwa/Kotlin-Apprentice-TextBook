package kotlin_apprentice.chapter_02_expressions_variables_constants.challenges

/**
 * Add parentheses to the following calculation. The parentheses should show the
 * order in which the operations are performed and should not alter the result of
 * th calculation.
 *      5 * 3 - 4 / 2 * 2
 */

fun main() = println((5 * 3) - ((4 / 2) * 2))